# MX Walk Reconciliation — 2026-09-25

TASK-284. The MX walk over the amended 2026-09-07 estate: is it complete,
current, and fail-closed?

## Journals used

| File | Path | mtime |
|------|------|-------|
| S3 amended | `work/stage/s3-icp-amended-PRODUCTIVE-2026-09-07.jsonl` | 2026-09-23 17:31 UTC |
| MX walk | `work/stage/mx-amended-PRODUCTIVE-2026-09-07.jsonl` | 2026-09-23 17:48 UTC |
| MX cache | `work/mx-cache.json` | 2026-09-23 17:48 UTC |

Path is Claude's worktree (`resonate-group-automation/work/`), read-only.
No writes were made to production `work/`.

## The identity

```
S3 rows (24,404) == MX rows (24,241) + not-yet-walked (0) + out-at-S3 (163)
  = 24,404
  ASSERTION PASSED
```

**The walk is complete.** Zero rows remain un-walked. Every S3 row is
accounted for by exactly one outcome.

## The 24,404 vs 24,241 gap

**Explained.** 163 rows went OUT at S3 (the ICP amendment). 24,404 − 163 =
24,241. The gap is fully accounted for by name: the S3 amended journal
carries 24,404 rows, of which 163 have `verdict: "out"`, leaving 24,241 for
the MX walk.

## Per-verdict census (five outcomes)

| Status | Count | Email channel |
|--------|------:|---------------|
| known_allowed | 20,357 | open |
| known_blocked | 660 | closed |
| unknown_provider | 3,022 | open |
| dns_failure | 43 | open (held) |
| no_mx | 159 | closed |
| **Total** | **24,241** | |

Email channel: 23,422 open, 819 closed.

**Five outcomes reported separately. Four buckets would be a fail.**

## unknown_provider split

| Cause | Count |
|-------|------:|
| Resolved MX we do not recognise | 3,022 |
| Resolver that did not answer | 0 |

**All 3,022 are in the MX cache with non-empty `mx_records`.** Zero are
resolver silence. The two categories are NOT being merged. A resolver
failure is `dns_failure` (43 domains, none cached by design), and
`unknown_provider` is exclusively "resolved but unrecognised".

The `dns_failure` domains are never cached (`src/mx.py` line ~510:
"A DNS failure is not cached: it is a transient condition"). Confirmed:
0 of 43 `dns_failure` domains appear in the cache.

## Sample domains per verdict

Real domains with the MX hostnames that produced the verdict. Domains are
companies, not people.

### known_blocked — 06marketing.com
```
MX: mx1-us1.ppe-hosted.com, mx2-us1.ppe-hosted.com
Provider: Proofpoint (ppe-hosted.com suffix match)
```

### unknown_provider — 100w.com
```
MX: mx00.1and1.com, mx01.1and1.com
Provider: unrecognised (1and1.com / IONOS — not in GATEWAYS or MAILBOX_HOSTS)
```

### dns_failure — 360mpr.com
```
MX: NOT IN CACHE (dns_failure is never cached by design)
Both resolvers (1.1.1.1, 8.8.8.8) did not answer within timeout.
Channel is HELD, not allowed.
```

### no_mx — 2patro.com
```
MX: [] (empty — domain publishes no MX record)
The domain accepts no mail.
```

### known_allowed — 0to9.nl
```
MX: 0to9-nl.mail.protection.outlook.com
Provider: Microsoft 365 (mail.protection.outlook.com suffix match)
```

## dns_failure: the channel is held, not allowed

`dns_failure` count: **43**.

The S5 gate refuses `dns_failure` at two layers:

### Layer 1: The walk's skip logic
`scripts/stage_mx_amended.py` line ~75:
```python
skip = status in (mx.KNOWN_BLOCKED, mx.NO_MX)
```
`dns_failure` is NOT in this tuple, so `email_channel = True` (held, not
closed). The channel is preserved for re-ask.

### Layer 2: The S5 gate's MX_OK check
`scripts/stage_s5_verify.py` line 120:
```python
MX_OK = ("known_allowed", "unknown_provider")
```
`scripts/stage_s5_verify.py` line 190:
```python
and row.get("mx") in MX_OK
```
`"dns_failure"` is NOT in `MX_OK`. The row is refused.

**Both layers agree:** the walk holds the channel (does not close it), and
the gate refuses the row (does not let it proceed). A `dns_failure` row
cannot reach an allowed state through the journal or the gate.

Test: `tests/test_a_dns_failure_never_reads_as_allowed.py` — 30 tests, all
passing.

## Null-mx rows after the walk

**Zero.** The walk completed fully: 24,241 of 24,241 non-out rows have been
answered. No row carries a null `mx` after the walk.

The task description mentioned rows carrying null `mx` from the offline
re-judge that "never asked." That condition has been resolved: the amended
walk filled every row.

## Label-boundary rule

`src/mx.py` `_matches()` (line ~200): label-boundary suffix match, never
substring.

```python
def _matches(host, suffix):
    return host == suffix or host.endswith("." + suffix)
```

Asserted through `mx.classify()`:

| Hostname | Suffix | Match? |
|----------|--------|--------|
| `mx1.pphosted.com` | `pphosted.com` | ✓ (proofpoint) |
| `notpphosted.com.evil.test` | `pphosted.com` | ✗ (None) |
| `pphosted.com.evil.test` | `pphosted.com` | ✗ (None) |
| `mymimecast.net` | `mimecast.com` | ✗ (None) |
| `eu-smtp-inbound-1.mimecast.com` | `mimecast.com` | ✓ (mimecast) |

The rule holds end to end through the walk's verdict.

## Resume proof

The walk is resumable and incremental by design.
`scripts/stage_mx_amended.py` reads the journal on start:
```python
done = {r["domain"]: r for r in rows(JOURNAL)}
todo = [r for r in src if r.get("verdict") != "out" and r["domain"] not in done]
```

Re-running the walk today:
- S3 rows (non-out): 24,241
- Already in MX journal: 24,241
- **Would re-ask: 0**

The walk is complete. Killing and restarting it re-asks nothing.

## S5 eligibility

The S5 gate requires ALL THREE conditions:
1. `verdict == "in"`
2. `email_channel is True`
3. `mx in MX_OK`

| | Count |
|--|------:|
| **Pass S5 gate** | **18,955** |
| Fail: verdict != "in" (flagged) | 4,629 |
| Fail: email_channel is False | 628 |
| Fail: mx not in MX_OK (dns_failure) | 29 |
| **Total** | **24,241** |

The 4,629 "flagged" rows are ICP-flagged at S3 and are refused by the
verdict check before the MX check is reached. The 628 email_channel=False
rows are known_blocked and no_mx domains with verdict "in". The 29
dns_failure rows with verdict "in" are refused by the `mx in MX_OK` check
(the remaining 14 dns_failure rows have verdict "flagged" and are caught
earlier).

## Findings

1. **The walk is complete.** 24,241 of 24,241 non-out rows answered. Zero
   null-mx rows remain. The identity holds exactly.

2. **The 24,404 vs 24,241 gap is explained.** 163 rows went OUT at S3.

3. **The 3,022 unknown_provider are all resolved MX we don't recognise.**
   Zero are resolver silence. The split is clean.

4. **dns_failure (43 domains) is correctly held and refused.** Not cached
   by design, not in MX_OK, refused by S5 gate at line 190.

5. **The label-boundary rule holds.** Substring attacks are rejected.

6. **The walk is resumable.** Re-running re-asks nothing.

## Risks

- The MX cache and journals are from 2026-09-23. DNS results may have
  changed for some domains since then. The `cache_days: 7` policy means
  entries older than 7 days would be re-resolved on the next run.
- The 43 `dns_failure` domains are not cached and would be re-asked on the
  next run. Some may now resolve.

## What requires operator action

- **None for this task.** The walk is complete, the gate is correct, and the
  five outcomes are properly distinguished. If the operator wants to re-probe
  the 43 `dns_failure` domains or the 3,022 `unknown_provider` domains, that
  is a separate decision.
