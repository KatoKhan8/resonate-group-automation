# Red test enumeration — 2026-09-16

TASK-164. Every test that was red when the suite ran, classified as (a) stale
allowlist or (b) test is right.

## Suite exit code before fixes

Non-zero. Six failing tests across two modules.

## (a) Stale allowlist — code is right, test fell behind

### 1. test_the_write_surface_is_exactly_this_and_nothing_else

**Module:** `tests/test_the_heyreach_write_contract.py`
**Route missing:** `/list/AddLeadsToListV2`
**Why it is (a):** TASK-158 added the route to `heyreach.WRITE_ROUTES` as a
real provider write (the list-add schema). The closed-set guard in this test
asserts the exact set of write routes; adding the route to the module without
updating the test left the guard red. The route is real and deliberate.
**Fix:** Added `"/list/AddLeadsToListV2"` to the expected set.

### 2. test_every_http_write_in_the_repository_is_declared

**Module:** `tests/test_nothing_writes_to_a_provider.py`
**Scripts undeclared:** 7

| Script | Verb | Reason |
|--------|------|--------|
| `scripts/provider_truth.py` | POST | Read-only. HeyReach spells reads as POST (GetAll campaigns, GetAll LI accounts). |
| `scripts/sender_capacity.py` | POST | Read-only. Reads sender estate limits from HeyReach. |
| `scripts/task158_probe2.py` | POST | Schema probe. Posts to empty list 940797, no campaign attached. |
| `scripts/task158_probe3.py` | POST | Schema probe. Same empty list. |
| `scripts/task158_probe_list_schema.py` | POST | Schema probe. Same empty list. |
| `scripts/task158_verify_schema.py` | POST | Schema probe. Same empty list. |
| `scripts/task166_grok_measurement.py` | POST | Measurement. Calls xAI Responses API for Grok research comparison. |

**Why each is (a):** Each script is a legitimate measurement or probe tool.
None is prospect-facing. The test's own docstring says "If it is a read a
vendor spells as POST, add it to ALLOWED with the reason." The ALLOWED set
had not been updated to register them.
**Fix:** Added all 7 to ALLOWED with reasons.

## (b) Test is right — real data in tracked files

### 3. test_every_email_address_is_on_a_reserved_domain

**Module:** `tests/test_fixture_hygiene.py`
**Hits:** 4 addresses on non-reserved domains in test files:
- `tests/test_bison_prewrite_check.py`: `test.com`
- `tests/test_company_evidence_cache.py`: `acme-test.com`
- `tests/test_hold_reasons.py`: `b.com`, `d.com`

**Why it is (b):** The rule is absolute — no allowlist, no exemption. `test.com`,
`acme-test.com`, `b.com` and `d.com` are real domains that resolve. Test
fixtures should use `.test`, `.example`, or `example.com`. The test is
correct to flag them. Not fixed here; the test files need their fixtures
rewritten to use reserved domains.

### 4. test_no_linkedin_url_with_real_vanity_name

**Module:** `tests/test_fixture_hygiene.py`
**Hit:** `docs/qwen-tasks/REVIEW/TASK-158-...md`: `brookebaron`
**Why it is (b):** A real LinkedIn vanity name in a tracked document. The test
is correct. The document needs the vanity redacted or hashed.

### 5. test_no_real_client_prospect_or_roster_domain

**Module:** `tests/test_fixture_hygiene.py`
**Hits:** 73 real domains across docs/ and scripts/ files.
**Why it is (b):** Real client, prospect and roster domains in tracked files.
The test is correct. These files need the domains redacted or hashed.

### 6. test_no_real_person_or_client_named

**Module:** `tests/test_fixture_hygiene.py`
**Hits:** 103 real names across docs/ and scripts/ files.
**Why it is (b):** Real person and client names in tracked files. The test is
correct. These files need the names redacted or hashed.

## Modules confirmed green

- `test_invariants` — 80 tests, OK
- `test_audit` — 16 tests, OK
- `test_the_factory_verbs_exist_and_are_sealed` — 13 tests, OK
- `test_two_campaigns_do_not_collide_at_the_provider` — 58 tests, OK
- `test_what_the_provider_actually_did` — 19 tests, OK
- `test_blitz`, `test_providers`, `test_discovery`, `test_source_reliability`,
  `test_the_client_icp_is_structural` — 320 tests, OK

## Suite exit code after fixes

`test_the_heyreach_write_contract` — 23 tests, OK (4 skipped)
`test_nothing_writes_to_a_provider` — 6 tests, OK
`test_fixture_hygiene` — 13 tests, 4 failures (unchanged, all (b))
