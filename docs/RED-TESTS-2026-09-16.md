# Red tests enumerated 2026-09-16, TASK-164

## Method

Ran the four modules TASK-157 and TASK-158 named, plus adjacent modules with
closed-set guards. Each red test was classified as (a) stale allowlist or
(b) the rule is right and the code is wrong.

## Red tests found: 5

### (a) Stale allowlist — fixed

1. **`test_the_write_surface_is_exactly_this_and_nothing_else`**
   (`tests/test_the_heyreach_write_contract.py`)
   - `/list/AddLeadsToListV2` is on `heyreach.WRITE_ROUTES` but not in the
     expected set. TASK-158 added the route; this test was not updated.
   - **Fix:** added `/list/AddLeadsToListV2` to the expected set.

2. **`test_every_http_write_in_the_repository_is_declared`**
   (`tests/test_nothing_writes_to_a_provider.py`)
   - 7 scripts issue POST calls not in ALLOWED:
     - `scripts/provider_truth.py` — HeyReach reads spelled as POST
       (`/campaign/GetAll`, `/li_account/GetAll`)
     - `scripts/sender_capacity.py` — HeyReach reads spelled as POST
       (`/li_account/GetAll`)
     - `scripts/task158_probe2.py` — TASK-158 probe for `/list/AddLeadsToListV2`
     - `scripts/task158_probe3.py` — TASK-158 probe
     - `scripts/task158_probe_list_schema.py` — TASK-158 schema probe
     - `scripts/task158_verify_schema.py` — TASK-158 schema verification
     - `scripts/task166_grok_measurement.py` — xAI responses API (intelligence
       read, same pattern as `src/providers/xai.py`)
   - None are prospect-facing writes. All are diagnostic scripts or reads.
   - **Fix:** added all 7 to ALLOWED with reasons.

### (b) Rule is right, code is wrong — reported as findings, not fixed

3. **`test_every_email_address_is_on_a_reserved_domain`**
   (`tests/test_fixture_hygiene.py`)
   - `tests/test_company_evidence_cache.py`: `acme-test.com`
   - `tests/test_hold_reasons.py`: `b.com`, `d.com`
   - These are test fixtures using non-reserved domains. The rule is right:
     any address on a domain that can resolve is an address somebody could
     be mailed at. The fixtures should use `.test` or `.example` domains.

4. **`test_no_real_client_prospect_or_roster_domain`**
   (`tests/test_fixture_hygiene.py`)
   - 17 hits across `docs/BISON-COHORT-LIVE-2026-09-15.md`,
     `docs/EMAIL-CONTROL-SEQUENCE-2026-09-15.md`,
     `scripts/task147_*.py`, `scripts/task147_results.json`,
     `scripts/task159_variable_resolution.py`,
     `scripts/task167_resolve_and_render.py`.
   - Real client/prospect domains in tracked files. The rule is right.

5. **`test_no_real_person_or_client_named`**
   (`tests/test_fixture_hygiene.py`)
   - 42 hits across docs/, scripts/task147_*, scripts/task158_*,
     scripts/task159_*, scripts/task167_*, and
     `docs/qwen-tasks/REVIEW/TASK-158-*.md`,
     `docs/qwen-tasks/REVIEW/TASK-165-*.md`.
   - Real names and company tokens in tracked files. The rule is right.

## Suite exit codes

- **Before:** test_the_heyreach_write_contract FAILED (1),
  test_nothing_writes_to_a_provider FAILED (1),
  test_fixture_hygiene FAILED (3).
  test_invariants OK, test_audit OK.
- **After:** test_the_heyreach_write_contract OK,
  test_nothing_writes_to_a_provider OK,
  test_invariants OK, test_audit OK.
  test_fixture_hygiene still FAILED (3) — (b) cases, not this task's to fix.

## Files changed

- `tests/test_the_heyreach_write_contract.py` — added `/list/AddLeadsToListV2`
  to expected set
- `tests/test_nothing_writes_to_a_provider.py` — added 7 scripts to ALLOWED
