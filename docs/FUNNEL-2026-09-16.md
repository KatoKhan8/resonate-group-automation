# Funnel recomputed from live state, 2026-09-16

**Source:** `work/queue.jsonl` (Claude's worktree, the authoritative file)
**Record count:** 550
**Queue mtime:** 2026-09-16T05:23:26Z (12 minutes before this measurement)
**Run in flight:** No. The spend-ledger was also modified at 05:22:52Z, consistent
with a free-path run that completed. No Python process is currently running.
**Provisional:** No. The queue is stable.

## The three populations reconciled

Three numbers have been in circulation. Here is what each one actually is:

| Number | What it is | Source |
|--------|-----------|--------|
| **550** | Total records in the live queue AND the current snapshot | `work/queue.jsonl` (550 lines), `work/queue.snapshot.jsonl` (550 lines, stamped 2026-09-15T17:52:12Z from master cf23154) |
| **316** | The subset of 550 that were in `queued` state in the snapshot | These were the records with no company_facts, no research, no stages - the target of the free-path run (TASK-163, TASK-171). NOT a separate population; it is 316 of the 550. |
| **300** | Total records in the OLD snapshot from 2026-09-14T21:52:15Z | The estate grew from 300 to 550 between Sept 14 evening and Sept 15 afternoon (a new ingest of ~250 records). The PRODUCTION-DASHBOARD.md was generated from this old snapshot and carries the 300 number. |

**The trap the task warned about:** "PRELIMINARY_ICP 300" and "300 records exist"
were the same number for different reasons in the old snapshot. In the old
snapshot, 300 records had company_facts (the NORMALIZED predicate) AND 300
records existed total. Reading "300 reached PRELIMINARY_ICP" as progress was
a mistake - it was the total estate size, not a funnel stage count.

In the current live state, all 550 records have company_facts (the free-path
run added minimal `headcount_signal` facts to the 250 that previously had
none), so NORMALIZED is now 550/550 = 100%.

## The funnel

Each stage states its predicate explicitly. A stage counted by a different
predicate than last time is a different number, not a change.

| Stage | Count | % of 550 | Predicate |
|-------|------:|---------:|-----------|
| RECEIVED | 550 | 100.0% | All records in queue.jsonl |
| NORMALIZED | 550 | 100.0% | Record has non-empty `company_facts` dict |
| FREE_RESEARCH | 392 | 71.3% | Record has non-empty `research` list |
| PRELIMINARY_ICP | 537 | 97.6% | `stages.qualify.status` = `done` (ICP scoring ran) |
| QUALIFIED | 113 | 20.5% | `qualification.verdict.icp_status` = `qualified` |
| ICP_REVIEW | 216 | 39.3% | `qualification.verdict.icp_status` = `review` |
| ICP_REJECTED | 221 | 40.2% | `qualification.verdict.icp_status` = `rejected` |
| PERSON_DISCOVERY | 91 | 16.5% | Record has at least one contact in `contacts[]` |
| ENRICHMENT | 550 | 100.0% | `stages.enrich.status` is set (partial or done) |
| VERIFICATION | 87 | 15.8% | At least one contact has `verification.state` set |
| VERIFIED_SENDABLE | 67 | 12.2% | At least one contact has `verification.state=verified` AND `sendable=True` |
| CAMPAIGN_READY | 68 | 12.4% | Record has cadence assigned (non-empty `cadence` field) |
| GENERATED | 190 | 34.5% | `stages.generate.status` = `done` |
| APPROVAL | 35 | 6.4% | At least one `draft_approved` event in `events[]` |
| LIVE_ELIGIBLE | 38 | 6.9% | State in (verified, approved, queued) + cadence + sendable contact |

**Terminal states:**

| State | Count |
|-------|------:|
| DROPPED | 126 |
| HELD | 32 |

**Drop reasons:**

| Reason | Count |
|--------|------:|
| rejected at ICP: no person-level enrichment | 117 |
| no contact found at this domain | 9 |

**Current state distribution (where records ARE, not where they've been):**

| State | Count |
|-------|------:|
| queued | 315 |
| dropped | 126 |
| verified | 65 |
| held | 32 |
| drafted | 12 |

**Contact summary:**

| Metric | Count |
|--------|------:|
| Total contacts across all records | 277 |
| Contacts with verification state set | 239 |
| Verified contacts | 68 |
| Sendable contacts (verified + sendable=True) | 68 |
| Contacts without verification | 38 |
| Records with at least one contact | 91 |
| Records with at least one verified+sendable contact | 67 |

## Before/after the free-path run

The free-path run (`python -m src.run --spend --cap 0 --stage enrich qualify`)
was run over the 316 queued records. Here is what changed:

| Stage | Snapshot (before) | Live (after) | Delta |
|-------|------------------:|-------------:|------:|
| NORMALIZED | 300 (54.5%) | 550 (100.0%) | +250 |
| FREE_RESEARCH | 207 (37.6%) | 392 (71.3%) | +185 |
| PRELIMINARY_ICP | 281 (51.1%) | 537 (97.6%) | +256 |
| QUALIFIED | 113 | 113 | 0 |
| ICP_REVIEW | 66 | 216 | +150 |
| ICP_REJECTED | 121 | 221 | +100 |
| ENRICHMENT | 298 (54.2%) | 550 (100.0%) | +252 |
| PERSON_DISCOVERY | 91 | 91 | 0 |
| CAMPAIGN_READY | 68 | 68 | 0 |
| GENERATED | 190 | 190 | 0 |

The free-path run processed the 250 records that had no company_facts and no
research, adding minimal `headcount_signal` facts and free webfetch evidence.
It then ran ICP scoring on all of them. The result: 150 landed in `review`
(not enough evidence for a verdict), 100 were `rejected` (structural ICP
fail), and a handful may have been `qualified`. The 113 previously qualified
records were unchanged.

## The snapshot: recommend regeneration

`work/queue.snapshot.jsonl` is stale. It was taken at
2026-09-15T17:52:12Z from master cf23154, before the free-path run mutated
live state. The snapshot shows:
- 250 records without company_facts (now all 550 have them)
- 207 records with research (now 392)
- ICP review: 66 (now 216)
- States: queued=316, drafted=44, held=36, dropped=125, verified=26, approved=3

**Recommendation:** Regenerate the snapshot from the current live state, or
retire it. The snapshot has served its purpose (providing a read-only copy
for worker tasks) but is now a source of wrong numbers. A stale artefact
that multiple tasks have measured from is worse than a missing one.

The snapshot is written by an external process (not `durable_state.py`, not
`store.py`). To regenerate, copy `work/queue.jsonl` to
`work/queue.snapshot.jsonl` and update `work/queue.snapshot.STAMP`.

## The authoritative file

`work/queue.jsonl` in Claude's worktree
(`C:\Users\Zvonimir\Desktop\resonate-group-automation\work\queue.jsonl`) is
the authoritative file for live pipeline state. It is gitignored, lives
outside any branch, and is written by `src/store.py`. It has 550 records as
of 2026-09-16T05:23:26Z.

No other file is authoritative:
- `work/queue.snapshot.jsonl` is a read-only copy, now stale
- `docs/state/PRODUCTION-DASHBOARD.md` was generated from the old 300-record snapshot
- `docs/state/QUEUE-MANIFEST.json` says the queue is absent (it was generated from a worktree that doesn't have queue.jsonl)
