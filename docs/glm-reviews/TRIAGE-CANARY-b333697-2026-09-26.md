# TRIAGE — GLM canary readiness review, independently verified

**Reviewed SHA:** `b33369748a91435f23524ede40ad74b1c54fb7cf`
**Current `origin/master` at triage:** `95ea3d36` — and
`git diff --stat b3336974 95ea3d36` is **one documentation file**. The code is
byte-identical to what GLM reviewed, so nothing here is stale for the usual
reason. Every finding below was nevertheless reproduced by hand against
`origin/master`; none is accepted on GLM's word.

**Review artifact:** `review/glm-b333697` @ `db9c2b17`, **on origin** (verified
`git ls-remote`). Worktree `C:\Users\Zvonimir\glm-review\glm-review-b333697`
left in place.

**Runtime verification performed: NONE.** No provider-changing action was taken.
CODE-LEVEL VERIFIED is not runtime verified, and PARTIALLY REVIEWED is not green.

## THE TABLE

| Finding | GLM | Current master | Evidence (reproduced) | Existing task | Action | Owner | Blocks canary |
|---|---|---|---|---|---|---|---|
| **P0-1** EMAIL_RESUME non-facing: resume skips suppression re-check, Authorization, spend gate, ledger reservation | P0 | **CONFIRMED CURRENT** | `providerwrites.py:406` `("email", False, …)`; `:561` in `SUPPORTED`; `:2189` `if facing: revalidate`; `campaigns.py` never imports `eligibility`, its 17 `CHECKS` hold no suppression read; `orchestrator.py:660` passes **no** `expect_leads` | **TASK-331** | **amended, not duplicated** — adds the `expect_leads` gap, the T-3 negative control, the two stale docstrings | Qwen | **YES** |
| **T-3** `orchestrator.resume` docstring "re-checks everything" | test | **CONFIRMED CURRENT** | `orchestrator.py:617`, `:648` | TASK-331 | folded in | Qwen | with P0-1 |
| **P1-1** no model call is checked against any ceiling; unknown provider writes no row | P1 | **CONFIRMED CURRENT** | `grep spendledger src/llm.py` = 2 hits, both `record`, post-call, inside `except Exception: pass`; `:286` `if provider is None: return`; only `check` callers are `enrich.py:925` and the verification path | **TASK-346** | **amended** — `holding(…)` pre-call gate, narrowed except, named row for unknown provider | Qwen | no (P1) |
| **T-1** ceiling test proves `check` raises, not that a call is refused | test | **CONFIRMED CURRENT** | `test_a_model_call_writes_a_priced_ledger_row.py:184` calls `spendledger.check` by hand; no second `complete()` | TASK-346 | folded in as the closing acceptance | Qwen | no |
| **P1-2** LinkedIn provider timing is a second source of cadence truth | P1 | **CONFIRMED CURRENT** | literal delays at `providers/heyreach.py:1227-1263` and `heyreachfactory.py:404-409`; `heyreachfactory` imports `cadencelibrary` only for `CAP_INMAIL` (`:736`) | **TASK-343** | **retargeted** — its preview half is verified fixed; the sending half is not | Qwen | no, but blocks a truthful LinkedIn slice |
| **P1-3 / T-2** write audit cannot see the transport's own shape; two undeclared POSTs green today | P1 | **CONFIRMED CURRENT** | `CALLS` regex, 3 alternatives, no `Request`-object form; `web/oidc.py:359`, `socketmode.py:76`; neither in `ALLOWED` | none | **TASK-370 created** (smallest) | Qwen | no |
| **P1-4** `researchpack` has zero production consumers; two research stores | P1 | **CONFIRMED CURRENT** | only caller is `scripts/capture_researchpack.py:102`; live facts come from `packfacts.pack_for` (`bisonfactory.py:559`) | none | **operator decision, no task** — which store is canonical | Operator | no |
| Approval hash not enforced | historical | **FIXED at code level** | `bisonfactory.py:962-976`, `heyreachfactory.py:163-215`, negative controls in `test_the_fingerprint_does_not_cover_every_field.py` | TASK-328 | close at code level; **runtime unproven** | — | no |
| Preview LinkedIn cadence 1/3/8/14 | historical | **FIXED** | no such literal in `src/` or `scripts/`; `preview.py:215,234` project `step["day"]` | TASK-343 | recorded | — | no |

## STATUS LINES

- **CURRENT MASTER SHA:** `95ea3d36` (code identical to the reviewed `b3336974`).
- **P0 CONFIRMED:** 1 — P0-1, owned by **TASK-331**.
- **P0 SUPERSEDED / FIXED:** 0.
- **P1 CONFIRMED:** 4 — P1-1 (TASK-346), P1-2 (TASK-343), P1-3 (TASK-370),
  P1-4 (operator decision).
- **P1 SUPERSEDED / FIXED:** 0. Two *historical* blockers are fixed at code
  level: the approval hash and the preview cadence divergence.
- **UNVERIFIED REMAINING:** 4, and they stay unverified deliberately — no
  worker is spent on a suspicion. (1) the fingerprint does not cover recipient,
  send day or sender; (2) `FREE_MAIL` is not consulted when a research key is
  built; (3) LinkedIn-side pagination was not inspected; (4) `web/app.py:1586`
  `api.create_campaign` may reach provider staging outside the factories.
  **(3) is folded into TASK-371's pagination acceptance** as the cheap check.
- **RUNTIME VERIFICATION ITEMS** — for the production session, none doable
  under the freeze: (a) whether any running loop even executes this code — the
  ~15 loops import at process start and never reload, so compare each process's
  start time against module mtimes before believing any of this is live;
  (b) **P0-1 blast radius** — whether per-lead `EMAIL_STOP_LEAD` had already
  been pushed for anyone who replied during a pause; (c) reverse cross-channel
  stop, email reply stopping a LinkedIn sequence at the provider, never
  measured; (d) whether `researchpack`'s Apify actors are still being paid for.
- **APPROVAL IDENTITY:** sound at code level, with real negative controls.
  **Not** runtime-proven. The residual GLM states rather than raises: the
  fingerprint binds the words, not the address, the day or the sender.
- **SUPPRESSION / RESUME:** **broken, confirmed, P0.** TASK-331 owns it. What
  still protects a resume today: `reviewapproval.require(campaign_id)` inside
  `bison.resume_campaign`, and `EMAIL_RESUME`'s absence from `REPEATABLE`.
  Neither reads suppression.
- **SPEND CEILING:** the ledger's shape is right — `MissingCeiling` subclasses
  `BudgetExceeded`, an unreadable ledger raises. **The model path is not
  connected to it.** TASK-346 owns it.
- **ACCOUNT IDENTITY — REMAINING GAP, preserved and not expanded:** admission
  (`packfacts.identity_of`) and cache scoping (`researchpack/cache.key_for`) are
  proven fail-closed. **Not traced:** the joins in `enrich.py`, `companies.py`
  and `providers/contactout.py`, and the "one account plus 2-3 contacts"
  representation end to end. Enough proof for the first slice is the goal — one
  account, 2-3 decision makers, shared account research, correct
  company/domain/contact joins, no cross-account leakage. Not an architecture
  project.
- **CRITICAL PATH:** unchanged and running. The entrypoint question is now
  **TASK-369**; TASK-371 refreshes provider truth; 366/321/365/354/368 are being
  verified and cherry-picked. No in-progress work was stopped, reassigned or
  restarted for this review.
- **PRODUCTION ACTIONS TAKEN: NONE.** Nothing sent, activated, resumed, paused,
  enrolled or attached. One read was made — `scripts/provider_truth.py`,
  read-only, refreshing the HeyReach half of `docs/state/PROVIDER-CAMPAIGNS.json`
  at 2026-09-26T16:09Z. 493 untouched.

## OPERATOR DECISION ADDED

**Which research store is canonical — `packfacts` or `researchpack`?**
`researchpack` builds account research that no `src/` module consumes; the live
path reads `rec["research"]` through `packfacts`. Wiring one in is preferred to
deleting the other, and neither happens until you pick. Until then nobody pays
that question in code.
